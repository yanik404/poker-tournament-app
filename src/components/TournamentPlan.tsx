import { useMemo, useState } from 'react'; import { DENOMINATIONS, stackValue, totalUsage } from '../logic/chips'; import { createTeams, DEFAULT_FINANCE, mainPrizes, money, playerContribution, teamShare } from '../logic/finances'; import type { ChipStack, FinanceSettings, Level, Player, Settings } from '../types/tournament';
const formatBlind = (level: Level) => level.isBreak ? 'PAUSE' : `${level.smallBlind} / ${level.bigBlind}`;
export function TournamentPlan({ settings, players, stack, levels, onStart, onBack }: { settings: Settings; players: Player[]; stack: ChipStack; levels: Level[]; onStart: (finance: FinanceSettings) => void; onBack: () => void }) {
  const use = totalUsage(stack, settings.players); const [finance, setFinance] = useState<FinanceSettings>(DEFAULT_FINANCE); const teams = useMemo(() => createTeams(players, finance.teamSize), [players, finance.teamSize]); const prizes = mainPrizes(players, finance);
  const update = (field: keyof FinanceSettings, value: number) => setFinance(old => ({ ...old, [field]: value } as FinanceSettings));
  return <main className="screen plan"><button className="back" onClick={onBack}>‹ Zurück</button><h1>Dein Turnier</h1><section className="panel"><h2>Startstack pro Spieler</h2><div className="stack-value">{stackValue(stack).toLocaleString('de-CH')} <small>Chips</small></div><div className="chip-grid">{DENOMINATIONS.map(chip => <div key={chip}><b>{chip}</b><span>× {stack[chip]}</span></div>)}</div><h3>Gesamtverbrauch · {settings.players} Spieler</h3><div className="usage">{DENOMINATIONS.map(chip => <span key={chip}>{chip}: <b>{use[chip]}</b>/ {chip === 500 || chip === 1000 ? 50 : 100}</span>)}</div></section><section className="panel"><h2>Turnierkasse</h2><label className="money-input">Buy-in pro Spieler<input aria-label="Buy-in pro Spieler" type="number" min="0" step="1" value={finance.buyIn} onChange={event => update('buyIn', Math.max(0, Number(event.target.value)))}/><b>CHF</b></label><label className="select-label">Team-Modus<select aria-label="Team-Modus" value={finance.teamSize} onChange={event => update('teamSize', Number(event.target.value))}><option value={0}>Kein Team-Pot</option><option value={2}>2er-Teams</option><option value={3}>3er-Teams</option></select></label>{finance.teamSize > 0 && <><label className="money-input">Team-Pot pro Team<input aria-label="Team-Pot pro Team" type="number" min="0" step="1" value={finance.teamPot} onChange={event => update('teamPot', Math.max(0, Number(event.target.value)))}/><b>CHF</b></label><p className="hint">Teams werden in der Reihenfolge der Namen gebildet. Der Team-Pot wird im Team gleichmässig aufgeteilt.</p><div className="team-list">{teams.map(team => <div key={team.id}><b>{team.name}</b><span>{team.memberIds.map(id => players.find(player => player.id === id)?.name).join(' · ')}</span><small>{money(teamShare(team, finance))} pro Person</small></div>)}</div></>}<h3>Einzahlung diese Runde</h3><div className="payment-list">{players.map(player => <span key={player.id}>{player.name}<b>{money(playerContribution(player, teams, finance))}</b></span>)}</div><h3>Preisgeld · Hauptpot {money(finance.buyIn * players.length)}</h3><div className="prizes"><span>1. {money(prizes[0])}</span><span>2. {money(prizes[1])}</span><span>3. {money(prizes[2])}</span></div></section><section className="panel"><h2>Blind-Plan</h2><ol className="levels">{levels.map((level, index) => <li key={level.id} className={level.isBreak ? 'break' : ''}><span>{level.isBreak ? 'BREAK' : `Level ${index + 1}`}</span><b>{formatBlind(level)}</b><small>{Math.round(level.durationSeconds / 60)} Min.</small></li>)}</ol></section><button className="primary" onClick={() => onStart(finance)}>Turnier starten</button></main>;
}
